# DJANGO REST API СЕРВИС | DRF | Postgresql | Docker |

# 🚀 Быстрый запуск Docker

### 1. Клонировать репозиторий
```bash
git clone https://github.com/anywaymy/api_service.git
cd api_service
```

### 2. Запустить через Docker Compose
```bash
docker-compose up --build
```

### 3. Открыть API - http://localhost:8000/api/v1/questions/

# 🚀 Запуск без Docker

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/anywaymy/api_service.git
cd api_service
```

### 2. Создайте виртуальное окружение

Windows

```bash
python -m venv venv
venv\Scripts\activate
```

Linux/macOS

```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Примите миграции

```bash
python manage.py migrate
```

### 5. Создайте супер пользователя

```bash
python manage.py createsuperuser
```

### 6. Некотоыре изменения
### в .env обязательно поменяйте POSTGRES_HOST=db на POSTGRES_HOST=localhost

### 7. Запустите проект

```bash
python manage.py runserver
```

### 8. Открыть API - http://localhost:8000/api/v1/questions/

## 🔗 Рабочие API Endpoints

### 🔐 **Аутентификация (Auth)**
| Метод | Endpoint | Описание | Статус |
|-------|----------|----------|--------|
| **POST** | `/api/v1/auth/register/` | Регистрация | ✅ Работает |
| **POST** | `/api/v1/auth/login/` | Вход | ✅ Работает |
| **POST** | `/api/v1/auth/logout/` | Выход | ✅ работает |
| **POST** | `/api/v1/auth/me/` | текущий пользователь | ✅ работает |

### 📝 Вопросы (Questions)
| Метод | Endpoint | Описание | Статус |
|-------|----------|----------|--------|
| GET | `/api/v1/questions/` | Получить список вопросов | ✅ Работает |
| POST | `/api/v1/questions/` | Создать новый вопрос | ✅ Работает |
| GET | `/api/v1/questions/{id}/` | Получить вопрос с ответами | ✅ Работает |
| DELETE | `/api/v1/questions/{id}/` | Удалить вопрос (каскадно) | ✅ Работает |

### 💬 Ответы (Answers)
| Метод | Endpoint | Описание | Статус |
|-------|----------|----------|--------|
| POST | `/api/v1/questions/{id}/answers/` | Добавить ответ к вопросу | ✅ Работает |
| GET | `/api/v1/answers/{id}/` | Получить конкретный ответ | ✅ Работает |
| DELETE | `/api/v1/answers/{id}/` | Удалить ответ | ✅ Работает |

## ЧТО РЕАЛИЗОВАНО?
### Модели данных
### Админ панель
### Авторизация/Регистрация
### Права доступа

## ВАЖНО

Не стоит хранить .env файлик в публичных репозиториях, так как он может хранить секретные данные. Только если в учебных целях

